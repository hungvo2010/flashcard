import { Method } from "./Method";

export class PDF extends Method {
    path: string;
    constructor(path: string){
        super(path);
    }

    // render() {

    // }

    getHighlightText() {
        let highlight = "";
        return highlight;
    }

    sendInfo() {
        let highlight, expand, accountID, table
    }
}