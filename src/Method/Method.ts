export class Method {
    path: string;
    constructor(path: string){}

    render(url: string) {
        return url
    }

    getHighlightText() {
        let highlight = "";
        return highlight;
    }

    sendInfo() {
        let highlight, expand, accountID, table
    }
}